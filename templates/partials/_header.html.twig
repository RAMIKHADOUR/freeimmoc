<br><br><br>
<div class="mb-5">
    <div class=" fixed-top navbar mb-5">
        <div class="container-fluid">
            <div class="m-3">
                <a class="navbar-brand" href="{{ path('app_login')}}"><span id="free">Free</span><span id="immo">Immo</span></a>  
            </div>
           
            <div>
                <a class="text-decoration-none m-4 text-warning fs-5 fw-bolder" href="{{ path('app_login')}}">Accueil</a>
                <a class="text-decoration-none m-4 text-warning fs-5 fw-bolder" href="{{ path('app_search')}}">Consulter</a>
                <a class="text-decoration-none m-4 text-warning fs-5 fw-bolder" href="{{ path('app_inscription')}}">S'Inscrire</a>
                <a class="btn btn-warning text-decoration-none fs-6 fw-bolder text-primary" href="{{ path('app_inscription')}}"
                    role="button"><strong>+</strong>Deposer Une Annonce</a>
            </div>
        </div>
    </div>
</div>